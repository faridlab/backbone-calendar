//! CalendarEmployeeStatus API Integration Tests
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Tests the CalendarEmployeeStatus CRUD API endpoints.

use chrono::Utc;
use serde_json::{json, Value};
use uuid::Uuid;

use super::crud_test_base::{CrudTestConfig, GenericCrudTest, TestDataGenerator};
use crate::integration::framework::ApiTest;
use crate::integration::helpers::CommonUtils;

// ============================================================================
// CALENDAREMPLOYEESTATUS TEST DATA GENERATOR
// ============================================================================

/// Test data generator for CalendarEmployeeStatus
pub struct CalendarEmployeeStatusTestData;

impl TestDataGenerator for CalendarEmployeeStatusTestData {
    fn generate_create_payload(&self, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": Uuid::new_v4().to_string(),
            "calendar_id": Uuid::new_v4().to_string(),
            "employment_status": "permanent",
            "metadata": json!({}),
        })
    }

    fn generate_update_payload(&self, id: &str, _utils: &CommonUtils) -> Value {
        let now = Utc::now().to_rfc3339();
        json!({
            "id": id,
            "calendar_id": Uuid::new_v4().to_string(),
            "employment_status": "permanent",
            "metadata": json!({}),
        })
    }

    fn generate_invalid_payload(&self) -> Value {
        json!({
            // Missing required fields
        })
    }

    async fn seed_dependencies(&self, api: &ApiTest) -> Vec<(String, String)> {
        let mut deps: Vec<(String, String)> = Vec::new();
        if let Some(id) = super::crud_test_base::create_and_get_id(api, "/api/v1/calendars", &super::calendar_api_test::CalendarTestData).await {
            deps.push(("calendar_id".to_string(), id));
        }
        deps
    }
}

// ============================================================================
// CALENDAREMPLOYEESTATUS API TEST
// ============================================================================

/// CalendarEmployeeStatus API test suite
pub struct CalendarEmployeeStatusApiTest {
    inner: GenericCrudTest<CalendarEmployeeStatusTestData>,
}

impl CalendarEmployeeStatusApiTest {
    pub fn new() -> Self {
        let mut config = CrudTestConfig::new("/api/v1/calendar_employee_statuses", "CalendarEmployeeStatus");
        config.supports_soft_delete = true;
        Self {
            inner: GenericCrudTest::new(config, CalendarEmployeeStatusTestData),
        }
    }

    pub async fn run_all(&mut self) -> Vec<crate::integration::framework::TestResult> {
        self.inner.run_all().await
    }
}

impl Default for CalendarEmployeeStatusApiTest {
    fn default() -> Self {
        Self::new()
    }
}

// ============================================================================
// TESTS
// ============================================================================

#[cfg(test)]
mod tests {
    use super::*;

    #[tokio::test]
    async fn test_calendar_employee_status_crud() {
        let mut test = CalendarEmployeeStatusApiTest::new();
        let results = test.run_all().await;

        for result in &results {
            println!("{:?}", result);
        }

        // Check all tests passed
        let failed: Vec<_> = results.iter().filter(|r| !r.success).collect();
        assert!(failed.is_empty(), "Some tests failed: {:?}", failed);
    }
}

// ============================================================================
// CUSTOM TESTS
// ============================================================================

// <<< CUSTOM TESTS START >>>
// Add CalendarEmployeeStatus specific tests here
// <<< CUSTOM TESTS END >>>
