//! Repository trait for CalendarPosition aggregate
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! This trait defines the repository contract for the CalendarPosition aggregate.
//! Implementation is in the infrastructure layer.

use async_trait::async_trait;
use anyhow::Result;
use uuid::Uuid;

use crate::domain::entity::CalendarPosition;

/// Pagination parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct CalendarPositionPaginationParams {
    pub page: u32,
    pub per_page: u32,
}

impl CalendarPositionPaginationParams {
    pub fn new(page: u32, per_page: u32) -> Self {
        Self { page, per_page }
    }

    pub fn offset(&self) -> u64 {
        ((self.page.saturating_sub(1)) * self.per_page) as u64
    }

    pub fn limit(&self) -> u64 {
        self.per_page as u64
    }
}

/// Paginated result for list queries
#[derive(Debug, Clone)]
pub struct CalendarPositionPaginatedResult {
    pub data: Vec<CalendarPosition>,
    pub total: u64,
    pub page: u32,
    pub per_page: u32,
}

/// Filter parameters for list queries
#[derive(Debug, Clone, Default)]
pub struct CalendarPositionFilter {
    pub calendar_id: Option<Uuid>,
    pub position_id: Option<Uuid>,
}

impl CalendarPositionFilter {
    /// Check if any filter is set
    pub fn has_filters(&self) -> bool {
        self.calendar_id.is_some() || self.position_id.is_some()
    }
}

/// Repository trait for CalendarPosition aggregate
///
/// This trait defines the persistence contract for CalendarPosition entities.
/// Implementations should be in the infrastructure layer.
#[async_trait]
pub trait CalendarPositionRepository: Send + Sync {

    // =========================================================================
    // Core CRUD Operations
    // =========================================================================

    /// Save a new calendar_position entity
    async fn save(&self, entity: &CalendarPosition) -> Result<CalendarPosition>;

    /// Find calendar_position by ID
    async fn find_by_id(&self, id: &str) -> Result<Option<CalendarPosition>>;

    /// Find all calendar_position entities
    async fn find_all(&self) -> Result<Vec<CalendarPosition>>;

    /// Update calendar_position by ID
    async fn update(&self, id: &str, entity: &CalendarPosition) -> Result<Option<CalendarPosition>>;

    /// Delete calendar_position by ID
    async fn delete(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Pagination
    // =========================================================================

    /// List calendar_position with pagination
    async fn list(&self, params: CalendarPositionPaginationParams) -> Result<CalendarPositionPaginatedResult>;

    /// List calendar_position with pagination and filters
    async fn list_with_filters(&self, params: CalendarPositionPaginationParams, filters: CalendarPositionFilter) -> Result<CalendarPositionPaginatedResult>;

    /// Count all calendar_position entities
    async fn count(&self) -> Result<u64>;

    /// Count calendar_position entities matching filters
    async fn count_with_filters(&self, filters: CalendarPositionFilter) -> Result<u64>;

    /// Check if calendar_position exists by ID
    async fn exists(&self, id: &str) -> Result<bool>;

    // =========================================================================
    // Soft Delete Operations
    // =========================================================================

    /// Soft delete calendar_position by ID
    async fn soft_delete(&self, id: &str) -> Result<bool>;

    /// Restore soft-deleted calendar_position by ID
    async fn restore(&self, id: &str) -> Result<Option<CalendarPosition>>;

    /// List soft-deleted calendar_position entities
    async fn list_deleted(&self, params: CalendarPositionPaginationParams) -> Result<CalendarPositionPaginatedResult>;

    /// Empty trash (permanently delete all soft-deleted entities)
    async fn empty_trash(&self) -> Result<u64>;

    // =========================================================================
    // Bulk Operations
    // =========================================================================

    /// Bulk save calendar_position entities
    async fn bulk_save(&self, entities: &[CalendarPosition]) -> Result<Vec<CalendarPosition>>;

    /// Bulk delete by IDs
    async fn bulk_delete(&self, ids: &[&str]) -> Result<u64>;
}
