//! Application service for CalendarEmployee entities
//!
//! Generated by metaphor-schema. Do not edit manually.
//!
//! Type alias over `GenericCrudService` — all CRUD logic lives in backbone-core.
//! For custom behaviour, create `calendar_employee_service_custom.rs` (preserved on regen).

use backbone_core::GenericCrudService;

use crate::presentation::dto::{CreateCalendarEmployeeDto, UpdateCalendarEmployeeDto};
use crate::domain::entity::CalendarEmployee;
use crate::infrastructure::persistence::CalendarEmployeeRepository;

/// Application service for CalendarEmployee entities.
///
/// All 12 standard CRUD operations (list, create, get, update, patch,
/// soft_delete, restore, empty_trash, bulk_create, upsert, find_by_id,
/// list_deleted) are provided by `GenericCrudService`.
///
/// Custom extensions go in `calendar_employee_service_custom.rs`:
///
/// ```rust,ignore
/// pub struct CalendarEmployeeServiceCustom {
///     inner: Arc<CalendarEmployeeService>,
///     // add domain-specific dependencies here
/// }
/// ```
pub type CalendarEmployeeService = GenericCrudService<
    CalendarEmployee,
    CreateCalendarEmployeeDto,
    UpdateCalendarEmployeeDto,
    CalendarEmployeeRepository,
>;

// <<< CUSTOM
// END CUSTOM
